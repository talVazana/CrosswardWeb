rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For local development, allow all reads and writes.
      allow read, write: if true;
    }
  }
}
