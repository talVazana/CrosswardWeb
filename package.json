{
  "name": "crossward",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start:frontend\" \"npm run start:firebase\"",
    "start:frontend": "cd frontend && npm run dev",
    "start:firebase": "firebase emulators:start --project crossward-local"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
